version: '3.8'

services:
  knightos-builder:
    image: localhost/knightos-builder:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: knightos-builder
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
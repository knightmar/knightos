version: '3.8'

services:
  knightos-builder:
    image: knightos-builder:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: knightos-builder
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true